# 各項分析指標的計算方式

## 一、分析單位與文字擷取

每一個學生網頁作品視為一個分析單位。程式先取得 HTML，再進行下列處理：

1. 解析 HTML 結構。
2. 移除 `script`、`style`、`noscript` 等不直接呈現給讀者的標籤。
3. 擷取可見文字，以單一空格合併連續空白。
4. 若作品使用 React／JavaScript 動態產生內容，則從同站子頁、字串常值及 JSX 文字節點補充可見內容。
5. 學生姓名、學號、個人作品網址與原始文本預覽在公開資料中移除，僅保留一次性匿名代碼。

> 資料表中的「總字數」實際上是正規化後的**可見文本字元數**，不是中文斷詞後的詞數。

---

## 二、媒體豐富度（1–4 分）

### 自動判定規則

| 分數 | 判定條件 | 作品型態 |
| --- | --- | --- |
| 1 | 沒有圖片或互動元素 | 純文字 |
| 2 | 至少有一個 `<img>` | 基礎圖文 |
| 3 | 出現 `canvas`、`svg`、`audio`、`video`、`iframe`，或程式中有明確互動事件 | 互動／多媒體 |
| 4 | 使用 Chart.js、D3、Three.js、Plotly、Leaflet 等進階視覺工具，或同時有多個互動元素 | 進階整合 |

第一個網頁作業有部分內容只存在於 React／JSX 原始碼中，因此若偵測到 `onClick`、`useState`、quiz、動畫或事件監聽程式，媒體分數最低補正為 3 分。

### 解讀

媒體分數代表「呈現與互動複雜度」，不直接代表學科內容正確性。

---

## 三、排版架構（1–4 分）

| 分數 | 判定條件 | 作品型態 |
| --- | --- | --- |
| 1 | 缺乏標題、段落或主要結構 | 流水帳／未分區 |
| 2 | 出現 `h1`、`h2`、`div`、`section`、`article`、`main` 等結構 | 基本段落 |
| 3 | 有 `nav`、`header`、`footer`，且至少有四個結構區塊 | 導覽與多區塊 |
| 4 | 程式包含 `fetch()`、Axios、XMLHttpRequest、API、GraphQL 等動態資料串接 | 動態資料／API |

若 React／JSX 原始碼含有導覽列、分頁、側欄或章節切換，排版分數最低補正為 3 分。

---

## 四、可見文本字元數

令正規化後的可見文本為 \(T\)，則：

\[
N = \operatorname{len}(T)
\]

其中 \(N\) 為資料表中的「總字數」。中文字、英文字母、數字與可見標點各依字元計算。

### 侷限

- 不同作業要求的篇幅不同，不能把字元數下降直接解讀為退步。
- 動態網頁的部分內容可能只在互動後顯示，程式擷取量仍可能低估。

---

## 五、專有名詞出現次數與密度

### 字典

字典包含地球物理與地震學核心詞，例如：

- 地震、震源、震央、斷層、板塊
- P 波、S 波、體波、表面波
- 直達波、反射波、折射波、走時、波速
- 地函、地核、地層、液化、防災
- seismic、refraction、reflection、epicenter

### 絕對次數

每一個字典詞在可見文本中獨立比對：

\[
C_{\text{term}}=\sum_{k=1}^{K}\operatorname{count}(term_k,T)
\]

中文採字串比對，英文不區分大小寫。不同詞條若互相包含，例如「地震」與「地震波」，可能各自計數。

### 每千字密度

\[
D_{\text{term}}=
\frac{C_{\text{term}}}{N}\times1000
\]

### 解讀

- 絕對次數反映作品使用多少學科語言。
- 密度反映學科詞彙在整體文字中的集中程度。
- 作品篇幅增加時，即使專有名詞次數增加，密度仍可能下降。

---

## 六、批判思考比例（啟發式）

### 句子切分

可見文本依句號、問號、驚嘆號與換行切分為句子。

### 關鍵訊號

批判／反思詞包括：

- 反思、發現、疑問、比較、差異
- 優點、缺點、限制、侷限、改進
- 我認為、為什麼、是否、或許
- however、limitation、insight、challenge

描述詞包括：

- 定義、公式、步驟、說明、介紹
- 包含、使用、流程、課程、教材

若某句的批判詞命中數高於描述詞，且至少命中一個批判詞，該句標記為批判思考句。

\[
R_{\text{critical}}=
\frac{N_{\text{critical sentences}}}
{N_{\text{all sentences}}}\times100\%
\]

### 解讀限制

本研究公開結果使用關鍵詞啟發式估計，不是人工內容分析，也不是心理量表。作業若本來要求課本介紹，低比例不代表表現不佳。

---

## 七、第一個網頁作業的主題涵蓋

依作業內容定義 11 個核心主題：

1. 地震學研究範疇與模型
2. 震源—傳播—觀測
3. Hazard 與 Risk
4. 能量衰減
5. 建築與共振
6. 液化與海嘯
7. 預報與預測
8. 地震空白區
9. 前兆與預測限制
10. 即時預警
11. 核試爆監測

每個主題有一組必要關鍵詞；全部必要詞都出現時，該主題記為 1，否則為 0。

\[
C_{\text{topic}}=\sum_{j=1}^{11}I_j
\]

\[
P_{\text{topic}}=
\frac{C_{\text{topic}}}{11}\times100\%
\]

「高涵蓋作品」定義為 \(C_{\text{topic}}\geq8\)。

主題涵蓋只表示概念有被提及，不代表敘述正確或理解深入。

---

## 八、內容發展指數（探索性）

先計算三項指標在 20 份第一個網頁作業中的百分等級：

- 可見文本字元數百分等級 \(PR_N\)
- 專有名詞絕對次數百分等級 \(PR_T\)
- 主題涵蓋數百分等級 \(PR_C\)

再等權平均：

\[
I_{\text{content}}=
\frac{PR_N+PR_T+PR_C}{3}
\]

此指數用來觀察內容量、學科語言和完整度是否方向一致，屬於事後探索指標，並非經驗證的正式量表。

---

## 九、縱向變化量

同一匿名學生在兩個時間點的變化：

\[
\Delta X=X_{\text{later}}-X_{\text{earlier}}
\]

百分比變化：

\[
G_X=
\left(
\frac{X_{\text{later}}}{X_{\text{earlier}}}-1
\right)\times100\%
\]

### 圖表使用

- 圖二：連接同一學生的上、下學期期末指標。
- 圖三：呈現兩學期期末五項指標的個別差值。
- 圖四：呈現下學期第一個網頁作業至期末的 18 位個別差值。
- 圖五：呈現先備組與新加入組從第一個作業至期末的平均軌跡。

兩次作業目的與篇幅要求不同，因此圖四、圖五只描述作品型態變化，不等同前後測因果效果。

---

## 十、高信度配對篩選

跨學期班級平均只納入：

1. 上、下學期作品均成功讀取；
2. 上學期靜態可見文本至少 400 字元；
3. 可由研究端私有鍵完成一對一配對。

符合條件者共 8 人。公開資料只保留一次性匿名代碼。

---

## 十一、組間效果量與探索性檢定

### Cliff's delta

\[
\delta=
\frac{\#(x_i>y_j)-\#(x_i<y_j)}
{n_xn_y}
\]

- \(\delta>0\)：第一組較高的配對比較較多。
- \(\delta<0\)：第二組較高的配對比較較多。
- \(\delta=0\)：兩組高度重疊。

### 精確秩排列檢定

將所有觀測值轉為平均秩，列舉固定組別人數下的所有可能分組，計算與實際秩和同樣或更極端的排列比例。

### Fisher 精確檢定

用於比較「高主題涵蓋／非高涵蓋」等二元比例，並回報勝算比（odds ratio）。

### 統計解讀

樣本小且不是隨機分組，所有檢定皆為探索性描述；不能據此推論上學期課程造成下學期表現差異。
